<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.karinastatinaite.pickyourownstory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PickYourOwnStory">
        <activity android:name=".TwoTwoActivity"></activity>
        <activity android:name=".TwoOne" />
        <activity android:name=".OneTwoTwoActivity" />
        <activity android:name=".OneTwoOne" />
        <activity android:name=".OneTwoActivity" />
        <activity android:name=".OneOneActivity" />
        <activity android:name=".OneThree" />
        <activity android:name=".ThreeActivity" />
        <activity android:name=".TwoActivity" />
        <activity android:name=".OneActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>